# Firebase Admin SDK Credentials (JSON string)
FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project-id",...}

# Google Drive API Credentials (JSON string)
GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"your-project-id",...}

# Google Drive - files uploaded to root (no folder needed)
# GOOGLE_DRIVE_FOLDER_ID=not_needed_for_root

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000